spring:
  application:
    name: workflow-messaging-engine

  datasource:
    url: jdbc:postgresql://localhost:5432/workflow_db
    username: sunho
    password: ""
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update      # 자동 테이블 생성/업데이트
      show-sql: true          # 콘솔에 SQL 쿼리 출력
      properties:
        hibernate:
          format_sql: true    # SQL 보기 좋게 정렬